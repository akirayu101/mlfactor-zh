---
title: "神经网络"
subtitle: "第 7 章交互式实验"
description: "比较线性基准与轻量多层感知机，并明确浏览器和本地环境的边界。"
resources:
  - data/data_ml_web.csv.gz
pyodide:
  packages:
    - pandas
    - numpy
    - matplotlib
    - scikit-learn
  resources:
    - data/data_ml_web.csv.gz
---

::: {.chapter-kicker}
第 7 章 · 混合运行
:::

# 从可解释基准开始

神经网络能够拟合非线性关系，但“更复杂”并不自动意味着“预测更好”。本实验采用严格的时间切分：

- 训练集：2015 年 12 月及以前；
- 测试集：2016 年 1 月及以后；
- 所有预处理参数仅从训练集估计；
- 线性模型作为必须击败的基准。

::: {.experiment-meta}
浏览器：scikit-learn MLP · 本地完整版本：后续接入 TensorFlow
:::

## 准备训练集与测试集

```{pyodide}
#| autorun: true
#| edit: false
#| startover: true
#| timelimit: 30
import pandas as pd
import numpy as np

from sklearn.metrics import mean_absolute_error
from sklearn.pipeline import make_pipeline
from sklearn.preprocessing import StandardScaler
from sklearn.linear_model import Ridge
from sklearn.neural_network import MLPRegressor

data = pd.read_csv(
    "data/data_ml_web.csv.gz",
    parse_dates=["date"],
)

features = [
    "Mkt_Cap_12M_Usd",
    "Pb",
    "Vol1Y_Usd",
    "Mom_11M_Usd",
]
split_date = pd.Timestamp("2016-01-01")

train = data[data["date"] < split_date]
test = data[data["date"] >= split_date]

X_train, y_train = train[features], train["R1M_Usd"]
X_test, y_test = test[features], test["R1M_Usd"]

print(f"训练集：{len(train):,} 条")
print(f"测试集：{len(test):,} 条")
```

## 先建立线性基准

```{pyodide}
#| timelimit: 30
ridge = make_pipeline(
    StandardScaler(),
    Ridge(alpha=1.0),
)
ridge.fit(X_train, y_train)
ridge_pred = ridge.predict(X_test)
ridge_mae = mean_absolute_error(y_test, ridge_pred)

print(f"Ridge 测试集 MAE：{ridge_mae:.5f}")
```

## 训练轻量多层感知机

这个模型刻意保持较小，以便在浏览器 CPU 中完成。你可以修改隐藏层规模、正则化强度或迭代次数。

```{pyodide}
#| caption: "浏览器端多层感知机"
#| timelimit: 60
hidden_layers = (12, 6)
regularization = 0.001
max_iterations = 60

mlp = make_pipeline(
    StandardScaler(),
    MLPRegressor(
        hidden_layer_sizes=hidden_layers,
        activation="relu",
        alpha=regularization,
        max_iter=max_iterations,
        early_stopping=True,
        validation_fraction=0.15,
        random_state=42,
    ),
)

mlp.fit(X_train, y_train)
mlp_pred = mlp.predict(X_test)
mlp_mae = mean_absolute_error(y_test, mlp_pred)

comparison = pd.Series({
    "Ridge": ridge_mae,
    "MLP": mlp_mae,
}, name="测试集 MAE")

comparison.round(5)
```

## 看模型是否真的改进

```{pyodide}
#| timelimit: 30
import matplotlib.pyplot as plt

ax = comparison.sort_values().plot.bar(
    color=["#2878b5", "#f2a541"],
    figsize=(7, 4),
)
ax.set(
    title="样本外误差比较",
    xlabel="模型",
    ylabel="平均绝对误差（越低越好）",
)
ax.tick_params(axis="x", rotation=0)
plt.tight_layout()
plt.show()
```

::: {.callout-important title="正确解读"}
如果 MLP 没有稳定击败 Ridge，这不是实验失败。金融横截面信号很弱，复杂模型更容易学习噪声；时间切分后的样本外结果比训练误差更重要。
:::

## 为什么完整版本仍然需要本地环境

浏览器原型适合小模型和教学实验，但原书第 7 章使用 TensorFlow/Keras。完整版本还需要：

- 全量数据与更长训练时间；
- 锁定的 TensorFlow 依赖；
- 可选 GPU；
- 模型检查点和训练日志；
- 不依赖浏览器内存的执行环境。

因此，本章最终会提供同一套叙述下的两条执行路径：网页内运行轻量模型，本地 Notebook 运行完整 TensorFlow 实验。

[下载第 7 章 Notebook](notebooks/chapter-07-neural-networks.ipynb){.btn .btn-outline-primary download="chapter-07-neural-networks.ipynb"}
